import React from 'react';
import './nav.scss'

const Nav = () => {
    return (
        <section className="nav">
            <button>About.</button>
            <button>Projects.</button>
            <button>CV.</button>
            <button>Connect.</button>
        </section>
    )
}

export default Nav
